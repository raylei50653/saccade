# High-accuracy profile — precision research and report generation
# Engine: yolo26l (higher accuracy, lower throughput)
# Tracking params identical to baseline; engine is the only difference.

engine: models/yolo/yolo26l_960_batch1.engine
match_thresh: 0.72
new_track_thresh: 0.35
