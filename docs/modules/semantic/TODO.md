# Semantic Relink — 模組 TODO

> 全局進度矩陣與 Baseline 見 [docs/TODO.md](../../TODO.md)。

## 🔄 Active

> **WIP=1 sole active：** occ-exit audit（#55）。 見 [DOC_MAINTENANCE.md](../../DOC_MAINTENANCE.md) § Workstream WIP； dashboard：[DEVELOPMENT.md §6](../../../DEVELOPMENT.md)。

- [ ] **occ-exit audit 條件化復活**（#55）— 復活條件：C++ occ_state + Cheb-GR graph 決策 + 13-型序列條件化。不動全域。同 #56 教訓：live 修正動作獎金需 ≥~4pp 才蓋得過複利懲罰。**Reference substrate 已就位**（2026-07-04）：`CleanFifoBank.samples_before(tid, occ_start)` = occ-exit audit 的 pre-episode clean reference，evaluator 已 wired（`--occ-audit-bank-reference` flag + `occ_exit_audit_lines_from_bank`），probe = `probe_occ_audit_bank_reference.py`；剩 Cheb-GR graph 決策 + 13-型序列條件化。substrate contract 見 [research/clean_fifo_bank_substrate_20260704.md](research/clean_fifo_bank_substrate_20260704.md)。

  **Substrate 抽出（2026-07-04）**：`CleanFifoBank` (`src/saccade/perception/eval/clean_fifo_bank.py`) 作為可重用 substrate，`extract_handover_embeddings` 已改薄 adapter（`recent` mode 走 planner + bank），occ-audit 接 bank reference（`occ_exit_audit_lines_from_bank` + `--occ-audit-bank-reference`）。消費者表 + 硬約束 + 排程 spec + 邊界見 [research/clean_fifo_bank_substrate_20260704.md](research/clean_fifo_bank_substrate_20260704.md)。Phase 2b probe（`TrackAppearanceBank`/`OutputAppearanceBank` FIFO 替換）= `probe_track_bank_fifo_replacement.py`；Phase 2c probe（forwarded embedding assoc cost）= `probe_forwarded_embedding_assoc_cost.py`。測試：`tests/unit/eval/test_clean_fifo_bank.py`（18 cases）、`tests/unit/eval/test_occ_audit_bank_reference.py`（5 cases）。

## 📋 Parked / next

> WIP=1：與 Active 互斥。開此項前須先收合或 park occ-exit。 規則見 [DOC_MAINTENANCE.md](../../DOC_MAINTENANCE.md) § Workstream WIP。

- [ ] **Sparse key-embedding bank（Cheb-GR 稀疏等價）— C++ async sidecar 化** — **parked（2026-07-09）**：不與 occ-exit 並進；需 async sidecar 立項（#57 禁 sync）。結案報告：[research/sparse_key_embedding_bank_20260704.md](research/sparse_key_embedding_bank_20260704.md)。probe GO（2026-07-04，registry #58 同 probe）：frozen substrate（`diag_m/s_no_reid_current_20260704`）上稀疏 bank 與 dense-50 的 offline handover 等價成立，**最佳形式 = visclean-gated clean-FIFO-20**（每 track 保留最近 20 個 clean 樣本；dIDF1 **+0.07/+0.05**，accept-set jaccard 0.85/0.72 全策略最高——死前樣本與 handover 比對時刻時間最近、外觀漂移最小，且 visclean gate 讓 FIFO 自然跳過髒的死亡期）。spread-10 也等價（+0.01/+0.01）但保真度較低；recent-10/-5 遞減（m −0.20/−0.48）→ **N=20 是 FIFO 安全點**。**抽取頻率可稀疏到每 3 個 clean 幀 1 次**：stridefifo-3-20 = −0.05/−0.03 等價（抽取成本 ÷3）；**K≥5 條件敏感不安全**（s：K5 −0.21、K10 −0.25、K15 −0.49；jaccard 隨 K 單調掉 0.85→0.33、handover 數膨脹 67→115 = cost 尺度漂移）。**硬約束（同 probe 否證）**：①bank 必須存**原始樣本**——mean1 單原型災難（−2.61/−1.14，距離塌縮 |dcost|med 0.066 → max_cost 操作點失效）、segmean-K 同預算輸原始樣本；②copy 出來的重複 embedding **不進 graph**——dupfill-5 不毒（+0.01/−0.06）但機制變保守（accepts 51→31、IDs 微升），bank 端一律存 unique；③**品質訊號選樣無效**——visclean gate 後 neighbor_iou/score/box_h 對 embedding 自洽性 spearman ≈0/+0.15/+0.1（m/s 復現），topscore-5/lowpol-5 都輸同預算 spread。**部署排程須 event-aware**：birth 後 decide 窗（5 幀）密集抽（min_head=2 需要，#56 教訓 budget 餓死 head），穩態 stride-3 + FIFO-20，死亡時 bank 即用。**保留條款**：本等價只驗證於 offline handover 操作點（max_gap 60）；長 gap relink 可能需要更久遠的樣本，屆時 FIFO-20+老樣本錨點另測。**已落地（Python 層，2026-07-04）**：`--cheb-gr-offline-bank-mode {spread,recent}` + `--cheb-gr-offline-bank-n`（default spread=現行 GO 不動）；一鍵 config `configs/modules/cheb_gr_offline_mnv4_fifo20.yaml`（recent-20，post-hoc bank crop 數 ~2.5×↓）。wired 路徑已在 m/s frozen substrate 端到端複驗與 probe 一致（m：11 +0.52/13 +0.26/05 +0.10/10 −0.10；s：05 +0.46/10 +0.23；IDs 持平）。**未做（刻意）**：C++ tracker 線上 FIFO ring buffer + stride-3 排程——前提是 async sidecar 立項（#57 禁 sync 抽取，現在 C++ bank 沒有消費者）。工具：`scripts/eval/diagnostics/probe_sparse_bank_equivalence.py`（策略×等價指標×訊號相關；`--strategies` 支援 spread/recent/stridefifo/dupfill/mean/segmean/topscore/lowpol）；`scripts/eval/run_offline_handover_ablation.py`（frozen substrate 上 handover 變體 A/B，變體鍵含 bank_mode/bank_n）。結果 JSON：`results/probe_sparse_bank{,_recency,_stridefifo}_{m,s}_20260704.json`。決策層 flags `--cheb-gr-offline-center-dist-veto/-pollution-veto/-neighbor-iou-max` 已接好 default off（veto ablation 全 noise 級或負，見 registry #58）。

## ✅ 已結案

### 收成（非 NO-GO）

- [x] **Cheb-GR offline handover candidate-edge 統計** → ⚪ **收成結案（2026-07-04）**：標註資料集 + 3-條件 applicability map 已建成；`best_cost` 為唯一 durable 判別訊號、veto/support 區跨條件穩定但**已被 handover 自身 max_cost gate 花光**，veto-cut / accept-tighten 兩個改 tracker 槓桿跨 3 條件皆否證 → offline handover 達 signal frontier。乾淨訊號/用途/依據見 [收成報告](research/chebgr_handover_signal_map_20260704.md)，registry [#58](../../reference/no_go_registry.md) 跨條件確認。以下為原分析記錄。原目標不是直接 grid search 最高 IDF1，而是建立 `archive tracklet A -> newborn tracklet B` 的離線標註資料集，分析 geometry / crowd pollution / appearance ambiguity 在不同區間的可信度。入口：
  - 產生 log：`uv run scripts/eval/mot17.py --preset mamba_whole_graph_m --detector SDP --double-buffer --module-lifecycle configs/modules/cheb_gr_offline_mnv4.yaml --cheb-gr-offline-log --output results/<run>`
  - 標註分析：`uv run scripts/eval/diagnostics/cheb_gr_offline_handover_report.py --handover-log results/<run>/_cheb_gr_offline_handover.csv --baseline-dir results/<no_handover> --pred-dir results/<run> --out-csv results/<run>/_cheb_gr_offline_handover_labeled.csv`。analyzer 現在會從 handover log 自動抓 seq 集合，不再預設限定 `*-SDP`。
  - 產生 registry/summary：同上加 `--registry-md results/<run>/parameter_registry.md --summary-json results/<run>/parameter_summary.json`；目前首份為 `results/diag_m_chebgr_offline_mnv4_h2_m005_log_20260704/parameter_registry.md` 與 `results/diag_m_chebgr_offline_mnv4_h2_m005_log_20260704/parameter_summary.json`。JSON schema `cheb_gr_offline_handover_summary/v1` 會保存 counts、feature buckets/zones、candidate rules、stdout gate search 的 `discovered_gates`（all-known single、accepted-known single、accepted-known two-feature）、policy simulation、combination matrices、pollution buckets/matrix，供跨 run diff 或下一輪 policy experiment 使用。
  - 跨 run 比較：`uv run scripts/eval/diagnostics/compare_handover_summaries.py results/<run_a>/parameter_summary.json results/<run_b>/parameter_summary.json --feature best_cost --feature neighbor_iou`，輸出 counts、candidate rules、discovered gates、policy simulation、pollution bucket、指定 feature bucket 的並列表，用來判斷訊號區間/組合是否跨條件穩定或只是單一 run artifact。
  - 收斂 applicability map：`uv run scripts/eval/diagnostics/synthesize_handover_applicability.py results/<run_a>/parameter_summary.json results/<run_b>/parameter_summary.json --out-md results/<map>.md --out-json results/<map>.json`。此步把多 run evidence 標成 `stable-veto` / `condition-sensitive-accept` / `stable-support` / `stable-danger` / `thin`，rule 層還要求 sequence coverage（預設 `min_seq_fraction=0.5`），避免把只集中在少數序列的 accept gate 誤當 default。
  - 工具防退化：`tests/unit/eval/test_cheb_gr_offline_handover_report.py` 用 synthetic MOT/GT fixture 驗證 accepted/rejected candidate 都會被標註、local tail/head label 可和 full-track majority 分離、endpoint pollution 會進 registry/summary；`tests/unit/eval/test_compare_handover_summaries.py` 驗證 summary comparator 會顯示 rule/pollution/feature bucket 漂移；`tests/unit/eval/test_synthesize_handover_applicability.py` 驗證 applicability map 會把 stable veto 與 drifting accept 分開。快速驗證：`.venv/bin/pytest -q tests/unit/eval/test_cheb_gr_offline_handover_report.py tests/unit/eval/test_compare_handover_summaries.py tests/unit/eval/test_synthesize_handover_applicability.py tests/unit/reid/test_cheb_gr_online.py tests/unit/test_eval_script_paths.py`。
  - Candidate Rule Map（hypothesis，不是 default gate）：accept 候選 = `best_cost<=0.25`（15/17，CI 0.66~0.97）、`margin>=0.12 && candidate_count>=5`（21/28，CI 0.57~0.87，LOO 0.70~0.79）、`best_cost<=0.35 && margin>=0.12`（20/27，CI 0.55~0.87，LOO 0.71~0.76）。support-only = `margin>=0.12`（單獨不穩）、`center_dist_norm<=0.5`（geometry support，不是 identity proof）。reject/veto context = `best_cost>=0.5`（4/211，CI 0.01~0.05）、`margin<=0.05`（6/194，CI 0.01~0.07）、`center_dist_norm>=2`（3/139，CI 0.01~0.06）。
  - Policy simulation（label-side，不改 tracker）：把 accept rule 當 whitelist 會提升 precision 但犧牲 recall，例如 `best_cost<=0.35` 在 accepted-known 內保留 22/28 正、7/26 錯，切掉 19/26 錯但也切 6/28 正；`margin>=0.12 && candidate_count>=5` 保留 21/28 正、6/26 錯。`best_cost>=0.5` 與 `margin<=0.05` 是 candidate-stage veto，目前不會切 accepted（accepted 已避開）；`center_dist_norm>=2` 是目前唯一直接命中 accepted 的 veto，會切 9/26 錯、誤切 2/28 正，值得作為「錯連保護」候選，但需序列/場景驗證。
  - 2026-07-04 首輪：`results/diag_m_chebgr_offline_mnv4_h2_m005_log_20260704` = IDF1 80.3 / IDs 311；candidate rows=323、accepted=57。以 local tail/head label 看 accepted known precision=28/54；full-track audit=30/56，且 accepted 內有 candidate full/tail mismatch=5、newborn full/head mismatch=3，證實 ID 污染需要獨立標記。單訊號：`best_cost` 最強（AUC≈0.91，越低越可信；`<0.25` bucket = 15/17 same_gt，Wilson CI 0.66~0.97；`0.25~0.35` = 7/13，CI 0.29~0.77，偏寬）、`margin` 次強（AUC≈0.83，但單獨 `margin>=0.12` 跨序列不穩，MOT17-04/09/11 仍低；`0.12~0.2` = 9/17，CI 0.31~0.74；`<0.05` 明顯 danger）、`center_dist_norm` 可輔助（`<0.5` support，`>2` danger）；`neighbor_iou` 目前不是單獨可靠 gate（AUC≈0.53），應作為 ReID/crop pollution context 與 `match_iou`、margin 組合看。穩定性：`best_cost<=0.35` = 22/30，leave-one-seq 0.72~0.75；`margin>=0.12 && candidate_count>=5` = 21/28，leave-one-seq 0.70~0.79；`best_cost<=0.35 && margin>=0.12` = 20/27，leave-one-seq 0.71~0.76。危險區也穩且 CI 低：`best_cost>=0.5` = 4/211、`margin<=0.05` = 6/194、`center_dist_norm>=2` = 3/139。污染證據：eligible rows 304 中 endpoint polluted 65/304；`neighbor_iou` 越高污染越多（<0.1: 16/112，0.3~0.5: 14/46，0.5~0.7: 8/21），`head_tail_neighbor_iou` 更明顯（<0.1: 7/74，0.5~0.7: 12/36，>0.7: 10/14）。`match_iou` 高 bucket 樣本很薄（0.5~0.9 僅 9 rows；CI 很寬，例如 0.5~0.7 same_gt 2/5 CI 0.12~0.77），不能單獨宣稱安全；目前結論是高幾何 IoU 需配合 neighbor/head-tail overlap 才能判斷是否污染。
  - 2026-07-04 cross-backbone check：新增真正不同分布的 `mamba_whole_graph`（s backbone）paired run：baseline `results/diag_s_no_reid_current_20260704` = IDF1 78.4 / IDs 425；Cheb-GR offline `results/diag_s_chebgr_offline_mnv4_h2_m005_log_20260704` = IDF1 78.9 / IDs 407。summary：candidate rows=402、accepted=75、known=331、accepted known precision=36/63=0.571，和 m 的 rows=323、precision=0.519 不同，代表這是有效 cross-condition evidence。比較輸出：`results/diag_compare_m_vs_s_chebgr_offline_20260704.md`；applicability map：`results/diag_applicability_m_s_chebgr_offline_20260704.md/json`。穩定訊號：`best_cost>=0.5` = `stable-veto`（m 4/211=0.02；s 7/229=0.03），`center_dist_norm>=2` = `stable-veto`（m 3/139=0.02；s 7/157=0.04），`margin<=0.05` = `stable-veto`（m 6/194=0.03；s 13/205=0.06），`center_dist_norm<0.5` = `stable-support`（m 16/45=0.36；s 28/66=0.42），`margin>=0.12` = `stable-support`（m 25/58=0.43；s 30/71=0.42），`neighbor_iou` 仍不是 identity gate。條件敏感：`best_cost<=0.25` rate 不低但序列覆蓋薄（m 15/17=0.88；s 13/18=0.72，rule class `condition-sensitive-accept`），`margin>=0.12 && candidate_count>=5` 從 21/28=0.75 降到 28/44=0.64（`condition-sensitive-accept`）；所以 accept rule 不能直接寫死成 default，應先作為 candidate whitelist/gray-zone priority，並搭配 sequence/condition validation。pollution trend 跨 backbone 保留：`head_tail_neighbor_iou` 由 <0.1 的 0.09 污染率升到 0.5~0.7 的 m 0.33 / s 0.35（`stable-high-pollution`）；>0.7 樣本較少但仍偏高（m 10/14=0.71；s 5/11=0.45）。
  - 2026-07-04 detector suffix check：重跑 `--detector FRCNN` 得到 `results/diag_m_no_reid_frcnn_20260704` 與 `results/diag_m_chebgr_offline_mnv4_h2_m005_log_frcnn_20260704`，metrics 與 SDP 完全相同；`scripts/eval/diagnostics/compare_detector_suffix_runs.py` normalized audit 顯示 MOT txt、handover CSV、labeled CSV、registry、summary JSON 全部 identical（323 rows / accepted 57 / correct 38 / wrong 266）。判定：此 eval preset 目前不提供真正 public-detector variation；不能把 FRCNN 當獨立驗證，只能證明 analyzer 已支援非 SDP seq suffix。**結構性結論（2026-07-04 code-level 確認，非只 empirical）**：`--detector` 在 `scripts/eval/mot17.py:132-142` 只是用 suffix 選 `MOT17-XX-{SDP,FRCNN,DPM}` 目錄，而這些目錄的影像相同、tracker 的 detections 一律來自 learned mamba detector（`mot17.py:267-299`），public `det/det.txt` 從不讀入 → 三種 suffix 必然 byte-identical，不是巧合。**因此 `--detector` 在本 harness 無法製造 cross-detector 條件，此線封死**。要取得真正第三個條件只能靠：①不同 backbone（m/s 已做）、②不同 sequence split / detection 操作點（需新 full eval）。每次 claimed cross-condition evidence 仍先跑 `compare_detector_suffix_runs.py` 防止重複樣本。
  - 2026-07-04 3rd genuine condition（confirm-gate regime）：先試 `--new-track-thresh` = 弱槓桿（m@nt0.20 IDF1 79.9/IDs 330、nt0.10 79.8/316 vs nt0.28 baseline 79.5/335，distinct-id sum 763→750/750、births ±2% 且**非單調**——因為真正 birth control 是 confirm gate `confirm_score_thresh 0.50 + confirm_streak 3`，new_track_thresh 只動 tentative pool 大多被 cull）。改用 `--confirm-score-thresh 0.30`（0.50→0.30）得到**真正不同 regime**：baseline `results/diag_m_no_reid_cs030_20260704` = IDF1 79.9 / IDs 411 / Rcll 86.0 / FP 3235（distinct-id sum 763→**885 +16%**、FP +50% = higher-recall/higher-pollution）；handover `results/diag_m_chebgr_offline_cs030_20260704` = IDF1 **80.5** / IDs 385、candidate rows **336**（vs cs0.50 的 323）。**3-way applicability map**（m@cs0.50 / s@cs0.50 / m@cs0.30，`results/diag_applicability_m_s_cs030_chebgr_offline_20260704.md/json`）在第 3 條件下確認：①**3 個 stable-veto 全數存活**，即使 FP +50% 也沒破 → 非 2-condition artifact：`best_cost>=0.5`（三條件 0.01~0.03，cs030 3/217）、`center_dist_norm>=2`（0.02~0.04，cs030 3/138）、`margin<=0.05`（0.03~0.06，cs030 6/198）；②**stable-support** `center_dist_norm<0.5`（0.36~0.42）、`margin>=0.12`（0.38~0.43）持平；③**accept rule 維持 condition-sensitive 且 spread 變寬**（`margin>=0.12 && candidate_count>=5` 0.75→cs030 0.61；`best_cost<=0.35 && margin>=0.12` 0.68~0.74；`best_cost<=0.25` 0.72~0.88）→ reinforced「不能寫死成 default」，只作 gray-zone priority；④**pollution 持穩**（`head_tail_neighbor_iou>=0.7` stable-high-pollution 0.45~0.71，cs030 0.60；`match_iou[0.1,0.3)` 0.31~0.40）。結論：veto/support 層跨 3 個真條件穩固。**但 2026-07-04 方案1 針對性再測否證「錯連保護」可用性**：在 cs0.30（pollution +50%、錯連最多的 regime）開 `--cheb-gr-offline-center-dist-veto 2.0`（唯一直接命中 accepted 的 veto）→ IDF1 80.5→80.6、IDs 385→386（handover baseline 79.9/411）。veto **active**（改動 2154 MOT 輸出行，vetoed handover 翻整條 tracklet，集中 seq-13/11/05）但 metric **net-neutral**＝切掉的 accepted-and-vetoed handover 對半正負相消。判定：**stable-veto 是 descriptive 不是 actionable**——handover 自身的 max_cost gate 已把可分離價值抽乾，顯式 veto 在最有利的髒 regime 也無淨值。連同 accept 層 condition-sensitive（不可寫死），item 2 兩個能改 tracker/輸出的槓桿（veto-cut / accept-tighten）跨條件皆已否證；offline handover 在此操作點已達 signal frontier，地圖價值是「解釋為何無法再擠」而非提供新 gate。
  - **收成（2026-07-04）**：乾淨訊號 / 用途 / 實驗依據已收束為自足報告 [research/chebgr_handover_signal_map_20260704.md](research/chebgr_handover_signal_map_20260704.md)（`best_cost` 為唯一 durable 判別訊號＋condition-stable veto/support 區＝sanity floor，但已被 handover 自身 max_cost gate 花光；不作 add-on gate）；registry [#58](../../reference/no_go_registry.md) 已補跨條件確認。此項可收（checkbox 待確認）。

### 全 NO-GO（code 保留 default off）

- [x] **Sync online ReID / tracker critical path** → ❌ NO-GO / cost-benefit 不成立。2026-07-04 重跑：無 ReID `mamba_whole_graph_m` = 79.5 / IDs 335 / ~241 FPS；修掉 live handover 污染後 `reid_mnv4_dynamic` = 79.7 / IDs 333 / ~193 FPS，收益僅 +0.2 IDF1 / -2 IDs、吞吐約 -20%。`relink_bridge_app_veto=0.20` 同分且 `bridge_veto=0`；極端 `veto=1.0` 僅 veto 33 pairs 且 IDs 341、reconnect 41.19%→40.13%。結論：ReID 不進同步 online critical path；除非同日 paired A/B 穩定 ≥+0.7~1.0 IDF1，否則不值得打破 double-buffer overlap。ReID 僅保留 async sidecar（drop-if-late cache/log）或 offline/post-hoc graph cleanup。
- [x] **phase-1 command 復查** → ⚪ 收斂到 offline-only / output relabel。當前比較基準為 `mamba_whole_graph_m + reid_mnv4_dynamic`（control `analysis_m_no_cheb_20260703` = IDF1 79.51 / IDs 335；`analysis_m_mnv4_tracker_reid_20260703` 同分）。phase-1 正向輸出來自 `results/mnv4_m_online_handover` 與 `results/mnv4_chebgr_c*` 家族：`mnv4_m_online_handover` = 80.20 / 313，`mnv4_chebgr_c050` = 80.42 / 291；其特徵是 evaluator 端 `--cheb-gr-offline-handover` 後處理重標（先跑完整輸出，再 crop/extract gallery，再 `causal_handover_lines`），不是 C++ live streaming claims。結論：phase-1 positive 是「不改變追蹤回饋迴路」的離線/輸出層 handover；不能復活 live claim 線。
- [x] **delayed-claim Cheb-GR 臂重掃** → ⚪ NO-GO / noise-level。row-major 修復後重算：control `analysis_m_semantic_delayed_claim_control_20260703` = 79.60 / 334；naive claim = 79.24 / 720（壞）；best guarded `analysis_m_semantic_cheb_claim_guarded_sim080_fixed_20260703` = 79.79 / 338（對 no-cheb +0.28、對 delayed-control +0.19，IDs 不降）。未達 >0.3pp 穩定 GO 門檻，且無 identity-switch 改善；維持 default off。
- [x] **Cheb-GR 路徑2 — offline tracklet merge** → ❌ NO-GO（AssA 0.0pp）。
  實作：`src/saccade/perception/eval/cheb_gr_merge.py`（時間分散取樣 N=20~100 + cheb_gr_kreciprocal 樣本級圖 + robust-min pool + greedy/frame-set 不相交 UnionFind）+ adapter `extract_tracklet_embeddings` + evaluator `run_eval` 掛載 + config `cheb_gr_merge_*`；7 unit tests。啟用 `--cheb-gr-merge-enabled`。
- [x] **Birth-time lost-bank relink（C++ GPU）** → ❌ NO-GO（無 λ 能降 IDs）。
  實作：`tracker_gpu.cu` GPU ring-buffer lost bank + Cheb-GR 兩階段 relink kernel（自適應門檻 + 速度搜捕圈）+ spawn 復活；`set_relink_params` + `--relink-*` flags。啟用 `--relink-enabled`。
- 診斷工具保留：`scripts/eval/reid_id_benchmark.py`（embedding 區分力 gate）、`scripts/eval/reconnect_rate.py`（重連率）、`scripts/train/reid_domain_probe.py`（MOT-域 head probe）。

> 註：Cheb-GR / relink core 程式碼物理上放在 `perception/reid/` 與 `tracking/`，但功能（re-ranking / 身份關聯）歸此模組；[reid](../reid/README.md) 維持暫緩。重啟條件＝MOT-域 ReID 特徵（見 [reid TODO](../reid/TODO.md)）。
