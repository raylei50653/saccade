{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://saccade.local/contracts/h2_successor_authorization_request_v1.json",
  "title": "H2 successor authorization request v1",
  "description": "A non-authoritative request for one owner-issued successor exactly-once measurement grant.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "authority",
    "execution_domain",
    "execution_id",
    "execution_semantics_projection_digest",
    "phase",
    "requested_issuer",
    "resolved_run_spec_digest",
    "schema"
  ],
  "properties": {
    "schema": {
      "const": "h2_successor_authorization_request_v1"
    },
    "authority": {
      "const": "exactly_once_measurement"
    },
    "execution_domain": {
      "$ref": "#/$defs/sha256"
    },
    "execution_id": {
      "$ref": "#/$defs/execution_id"
    },
    "execution_semantics_projection_digest": {
      "$ref": "#/$defs/sha256"
    },
    "phase": {
      "const": "a"
    },
    "requested_issuer": {
      "const": "research_owner"
    },
    "resolved_run_spec_digest": {
      "$ref": "#/$defs/sha256"
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "execution_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$"
    }
  }
}
