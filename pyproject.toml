[project]
name = "saccade"
version = "0.1.0"
description = "High-efficiency edge AI video perception system"
readme = "README.md"
requires-python = ">=3.12, <3.13"
dependencies = [
    "ultralytics",
    "numpy",
    "scipy",
    "opencv-python",
    "chromadb",
    "redis",
    "torch==2.11.0",
    "torchvision==0.26.0",
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "ruff",
    "pytest",
    "httpx",
    "nvidia-ml-py",
    "lap>=0.5.13",
    "mypy>=1.20.0",
    "pyyaml>=6.0.3",
    "anyio>=4.13.0",
    "pytest-asyncio>=1.3.0",
    "pytest-anyio>=0.0.0",
    "types-pyyaml>=6.0.12.20260408",
    "pygobject==3.48.2",
    "timm>=1.0.26",
    "onnxscript>=0.6.2",
    "transformers>=4.48.0",
    "accelerate>=1.13.0",
    "optimum[onnxruntime-gpu]>=2.1.0",
    "einops>=0.8.2",
    "onnxruntime-gpu>=1.24.4",
    "tensorrt-cu12==10.16.1.11",
    "pybind11>=3.0.3",
    "pytest-cov>=6.0.0",
    "optuna>=4.8.0",
    "optuna-dashboard>=0.20.0",
    "motmetrics>=1.4.0",
    "llama-index>=0.14.21",
    "llama-index-vector-stores-chroma>=0.5.5",
    "sqlalchemy>=2.0.38",
    "psycopg2-binary>=2.9.10",
    "sahi>=0.11.36",
    "mlflow>=3.2.0",
    "pycocotools>=2.0.11",
    "tabulate>=0.10.0",
]

# GPU media/NVDEC path only. Cloud runners / C++ core / default uv sync skip this
# (large CUDA wheel; useless without GPU). Local GPU hosts: `uv sync --extra dali`.
[project.optional-dependencies]
dali = [
    "nvidia-dali-cuda120>=2.0.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
packages = {find = {where = ["src"]}}

[tool.uv]
override-dependencies = [
    "onnxruntime==1.24.4",
]

[tool.mypy]
strict = true
ignore_missing_imports = true
exclude = ["tests/", "build/", "build-native-coverage/", "build-native-test/", "storage/", "runs/", "results/", "train/", "scratch/", "third_party/"]
mypy_path = "src"

[[tool.mypy.overrides]]
module = ["scripts.*"]
ignore_errors = true

[[tool.mypy.overrides]]
module = ["saccade.perception.temporal_yolo.*"]
ignore_errors = true

[[tool.mypy.overrides]]
module = [
    "build_fpn_reid",
    "saccade.media.dali_pipeline",
    "saccade.media.rtsp_dali_pipeline",
    "saccade.perception.detector_trt",
    "saccade.perception.eval.detection",
    "saccade.perception.eval.streaming",
    "saccade.perception.tracking.fpn_reid",
    "saccade.perception.tracking.fpn_reid_cuda",
    "saccade.perception.workbench",
]
ignore_errors = true

[tool.ruff]
exclude = ["tests/benchmarks/archive", "storage/", "runs/", "results/", "third_party/", "scratch/"]

[tool.ruff.lint]
ignore = ["E701"]

[tool.ruff.lint.per-file-ignores]
"scripts/**" = ["E402"]

[tool.pytest.ini_options]
addopts = "-m 'not research'"
testpaths = ["tests"]
python_files = ["test_*.py"]
asyncio_mode = "strict"
pythonpath = ["src"]
markers = [
    "gpu: tests that require a CUDA device or GPU-only native extension path",
    "dali: tests that require a CUDA device and nvidia-dali (NVDEC decode path)",
    "research: dated research / study-scoped tests under tests/research, excluded from the default collection (run with -m research)",
    "packet_bound: reads a sealed research evidence packet or frozen dated artifact",
]
